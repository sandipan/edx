box=FALSE)
x <- seq(-5,5,0.2)
y <- seq(-5,5,0.2)
s <- 1.5
z <- outer(x, y, function(x,y) 200/s^6/pi*(1-(x^2+y^2)/2/s^2)*exp(-(x^2+y^2)/2/s^2))
# Color palette (100 colors)
col.pal<-colorRampPalette(c("blue", "red"))
colors<-col.pal(100)
# height of facets
z.facet.center <- (z[-1, -1] + z[-1, -ncol(z)] + z[-nrow(z), -1] + z[-nrow(z), -ncol(z)])/4
# Range of the facet center on a 100-scale (number of colors)
z.facet.range<-cut(z.facet.center, 100)
persp(x, y, z,
theta = 30, phi = 30, expand = 0.2, scale=FALSE,
shade=NA, col=colors[z.facet.range], border="grey80",
box=FALSE)
x <- seq(-5,5,0.2)
y <- seq(-5,5,0.2)
s <- 1.5
z <- outer(x, y, function(x,y) 200/s^2/pi*(1-(x^2+y^2)/2/s^2)*exp(-(x^2+y^2)/2/s^2))
# Color palette (100 colors)
col.pal<-colorRampPalette(c("blue", "red"))
colors<-col.pal(100)
# height of facets
z.facet.center <- (z[-1, -1] + z[-1, -ncol(z)] + z[-nrow(z), -1] + z[-nrow(z), -ncol(z)])/4
# Range of the facet center on a 100-scale (number of colors)
z.facet.range<-cut(z.facet.center, 100)
persp(x, y, z,
theta = 30, phi = 30, expand = 0.2, scale=FALSE,
shade=NA, col=colors[z.facet.range], border="grey80",
box=FALSE)
x <- seq(-5,5,0.2)
y <- seq(-5,5,0.2)
s <- 1.5
z <- outer(x, y, function(x,y) 100/s^2/pi*(1-(x^2+y^2)/2/s^2)*exp(-(x^2+y^2)/2/s^2))
# Color palette (100 colors)
col.pal<-colorRampPalette(c("blue", "red"))
colors<-col.pal(100)
# height of facets
z.facet.center <- (z[-1, -1] + z[-1, -ncol(z)] + z[-nrow(z), -1] + z[-nrow(z), -ncol(z)])/4
# Range of the facet center on a 100-scale (number of colors)
z.facet.range<-cut(z.facet.center, 100)
persp(x, y, z,
theta = 30, phi = 30, expand = 0.2, scale=FALSE,
shade=NA, col=colors[z.facet.range], border="grey80",
box=FALSE)
x <- seq(-5,5,0.2)
y <- seq(-5,5,0.2)
s <- 1.5
z <- outer(x, y, function(x,y) 100/s^3/pi*(1-(x^2+y^2)/2/s^2)*exp(-(x^2+y^2)/2/s^2))
# Color palette (100 colors)
col.pal<-colorRampPalette(c("blue", "red"))
colors<-col.pal(100)
# height of facets
z.facet.center <- (z[-1, -1] + z[-1, -ncol(z)] + z[-nrow(z), -1] + z[-nrow(z), -ncol(z)])/4
# Range of the facet center on a 100-scale (number of colors)
z.facet.range<-cut(z.facet.center, 100)
persp(x, y, z,
theta = 30, phi = 30, expand = 0.2, scale=FALSE,
shade=NA, col=colors[z.facet.range], border="grey80",
box=FALSE)
x <- seq(-5,5,0.2)
y <- seq(-5,5,0.2)
s <- 1.5
z <- outer(x, y, function(x,y) 100/s^4/pi*(1-(x^2+y^2)/2/s^2)*exp(-(x^2+y^2)/2/s^2))
# Color palette (100 colors)
col.pal<-colorRampPalette(c("blue", "red"))
colors<-col.pal(100)
# height of facets
z.facet.center <- (z[-1, -1] + z[-1, -ncol(z)] + z[-nrow(z), -1] + z[-nrow(z), -ncol(z)])/4
# Range of the facet center on a 100-scale (number of colors)
z.facet.range<-cut(z.facet.center, 100)
persp(x, y, z,
theta = 30, phi = 30, expand = 0.2, scale=FALSE,
shade=NA, col=colors[z.facet.range], border="grey80",
box=FALSE)
x <- seq(-5,5,0.2)
y <- seq(-5,5,0.2)
s <- 1
z <- outer(x, y, function(x,y) 100/s^4/pi*(1-(x^2+y^2)/2/s^2)*exp(-(x^2+y^2)/2/s^2))
# Color palette (100 colors)
col.pal<-colorRampPalette(c("blue", "red"))
colors<-col.pal(100)
# height of facets
z.facet.center <- (z[-1, -1] + z[-1, -ncol(z)] + z[-nrow(z), -1] + z[-nrow(z), -ncol(z)])/4
# Range of the facet center on a 100-scale (number of colors)
z.facet.range<-cut(z.facet.center, 100)
persp(x, y, z,
theta = 30, phi = 30, expand = 0.2, scale=FALSE,
shade=NA, col=colors[z.facet.range], border="grey80",
box=FALSE)
x <- seq(-5,5,0.2)
y <- seq(-5,5,0.2)
s <- 1
z <- outer(x, y, function(x,y) 25/s^4/pi*(1-(x^2+y^2)/2/s^2)*exp(-(x^2+y^2)/2/s^2))
# Color palette (100 colors)
col.pal<-colorRampPalette(c("blue", "red"))
colors<-col.pal(100)
# height of facets
z.facet.center <- (z[-1, -1] + z[-1, -ncol(z)] + z[-nrow(z), -1] + z[-nrow(z), -ncol(z)])/4
# Range of the facet center on a 100-scale (number of colors)
z.facet.range<-cut(z.facet.center, 100)
persp(x, y, z,
theta = 30, phi = 30, expand = 0.2, scale=FALSE,
shade=NA, col=colors[z.facet.range], border="grey80",
box=FALSE)
x <- seq(-3,3,0.2)
y <- seq(-3,3,0.2)
s <- 1
z <- outer(x, y, function(x,y) 25/s^4/pi*(1-(x^2+y^2)/2/s^2)*exp(-(x^2+y^2)/2/s^2))
# Color palette (100 colors)
col.pal<-colorRampPalette(c("blue", "red"))
colors<-col.pal(100)
# height of facets
z.facet.center <- (z[-1, -1] + z[-1, -ncol(z)] + z[-nrow(z), -1] + z[-nrow(z), -ncol(z)])/4
# Range of the facet center on a 100-scale (number of colors)
z.facet.range<-cut(z.facet.center, 100)
persp(x, y, z,
theta = 30, phi = 30, expand = 0.2, scale=FALSE,
shade=NA, col=colors[z.facet.range], border="grey80",
box=FALSE)
plot.3d <- function(x, y, z) {
# Color palette (100 colors)
col.pal<-colorRampPalette(c("blue", "red"))
colors<-col.pal(100)
# height of facets
z.facet.center <- (z[-1, -1] + z[-1, -ncol(z)] + z[-nrow(z), -1] + z[-nrow(z), -ncol(z)])/4
# Range of the facet center on a 100-scale (number of colors)
z.facet.range<-cut(z.facet.center, 100)
persp(x, y, z,
theta = 30, phi = 30, expand = 0.2, scale=FALSE,
shade=NA, col=colors[z.facet.range], border="grey80",
box=FALSE)
}
x <- seq(-3,3,0.2)
y <- seq(-3,3,0.2)
s <- 1
z <- outer(x, y, function(x,y) 25/s^4/pi*(1-(x^2+y^2)/2/s^2)*exp(-(x^2+y^2)/2/s^2)) # -ve LOG
plot.3d(x, y, z)
plot.3d <- function(x, y, z) {
# Color palette (100 colors)
col.pal<-colorRampPalette(c("blue", "red"))
colors<-col.pal(100)
# height of facets
z.facet.center <- (z[-1, -1] + z[-1, -ncol(z)] + z[-nrow(z), -1] + z[-nrow(z), -ncol(z)])/4
# Range of the facet center on a 100-scale (number of colors)
z.facet.range<-cut(z.facet.center, 100)
#persp(x, y, z,
#      theta = 30, phi = 30, expand = 0.2, scale=FALSE,
#      shade=NA, col=colors[z.facet.range], border="grey80",
#      box=FALSE)
nlevels<-20
my_zlim <- range(z, finite = TRUE)
my_levels <- pretty(my_zlim, nlevels)
cols <- jet.colors(length(my_levels) - 1)
zzz <- cut(zz, breaks = my_levels, labels = cols)
persp(my_x, my_y, my_z, theta = -25, phi = 45, expand = 0.5,xlab="x",ylab="y",zlab="f(x,y)",col = as.character(zzz))
}
x <- seq(-3,3,0.2)
y <- seq(-3,3,0.2)
s <- 1
z <- outer(x, y, function(x,y) 25/s^4/pi*(1-(x^2+y^2)/2/s^2)*exp(-(x^2+y^2)/2/s^2)) # -ve LOG
plot.3d(x, y, z)
install.packages('pacman')
library(pacman)
library(pacman)
plot.3d <- function(x, y, z) {
# Color palette (100 colors)
col.pal<-colorRampPalette(c("blue", "red"))
colors<-col.pal(100)
# height of facets
z.facet.center <- (z[-1, -1] + z[-1, -ncol(z)] + z[-nrow(z), -1] + z[-nrow(z), -ncol(z)])/4
# Range of the facet center on a 100-scale (number of colors)
z.facet.range<-cut(z.facet.center, 100)
#persp(x, y, z,
#      theta = 30, phi = 30, expand = 0.2, scale=FALSE,
#      shade=NA, col=colors[z.facet.range], border="grey80",
#      box=FALSE)
nlevels<-20
my_zlim <- range(z, finite = TRUE)
my_levels <- pretty(my_zlim, nlevels)
cols <- jet.colors(length(my_levels) - 1)
zzz <- cut(zz, breaks = my_levels, labels = cols)
persp(my_x, my_y, my_z, theta = -25, phi = 45, expand = 0.5,xlab="x",ylab="y",zlab="f(x,y)",col = as.character(zzz))
}
x <- seq(-3,3,0.2)
y <- seq(-3,3,0.2)
s <- 1
z <- outer(x, y, function(x,y) 25/s^4/pi*(1-(x^2+y^2)/2/s^2)*exp(-(x^2+y^2)/2/s^2)) # -ve LOG
plot.3d(x, y, z)
p_load(tidyverse)
p_load(mvtnorm)
p_load(cowplot)
p_load(gridGraphics)
p_load(GA)
plot.3d <- function(x, y, z) {
# Color palette (100 colors)
col.pal<-colorRampPalette(c("blue", "red"))
colors<-col.pal(100)
# height of facets
z.facet.center <- (z[-1, -1] + z[-1, -ncol(z)] + z[-nrow(z), -1] + z[-nrow(z), -ncol(z)])/4
# Range of the facet center on a 100-scale (number of colors)
z.facet.range<-cut(z.facet.center, 100)
#persp(x, y, z,
#      theta = 30, phi = 30, expand = 0.2, scale=FALSE,
#      shade=NA, col=colors[z.facet.range], border="grey80",
#      box=FALSE)
nlevels<-20
my_zlim <- range(z, finite = TRUE)
my_levels <- pretty(my_zlim, nlevels)
cols <- jet.colors(length(my_levels) - 1)
zzz <- cut(zz, breaks = my_levels, labels = cols)
persp(my_x, my_y, my_z, theta = -25, phi = 45, expand = 0.5,xlab="x",ylab="y",zlab="f(x,y)",col = as.character(zzz))
}
x <- seq(-3,3,0.2)
y <- seq(-3,3,0.2)
s <- 1
z <- outer(x, y, function(x,y) 25/s^4/pi*(1-(x^2+y^2)/2/s^2)*exp(-(x^2+y^2)/2/s^2)) # -ve LOG
plot.3d(x, y, z)
plot.3d <- function(x, y, z) {
# Color palette (100 colors)
col.pal<-colorRampPalette(c("blue", "red"))
colors<-col.pal(100)
# height of facets
z.facet.center <- (z[-1, -1] + z[-1, -ncol(z)] + z[-nrow(z), -1] + z[-nrow(z), -ncol(z)])/4
# Range of the facet center on a 100-scale (number of colors)
z.facet.range<-cut(z.facet.center, 100)
#persp(x, y, z,
#      theta = 30, phi = 30, expand = 0.2, scale=FALSE,
#      shade=NA, col=colors[z.facet.range], border="grey80",
#      box=FALSE)
nlevels<-20
my_zlim <- range(z, finite = TRUE)
my_levels <- pretty(my_zlim, nlevels)
cols <- jet.colors(length(my_levels) - 1)
zzz <- cut(z.facet.center, breaks = my_levels, labels = cols)
persp(my_x, my_y, my_z, theta = -25, phi = 45, expand = 0.5,xlab="x",ylab="y",zlab="f(x,y)",col = as.character(zzz))
}
x <- seq(-3,3,0.2)
y <- seq(-3,3,0.2)
s <- 1
z <- outer(x, y, function(x,y) 25/s^4/pi*(1-(x^2+y^2)/2/s^2)*exp(-(x^2+y^2)/2/s^2)) # -ve LOG
plot.3d(x, y, z)
plot.3d <- function(x, y, z) {
# Color palette (100 colors)
col.pal<-colorRampPalette(c("blue", "red"))
colors<-col.pal(100)
# height of facets
z.facet.center <- (z[-1, -1] + z[-1, -ncol(z)] + z[-nrow(z), -1] + z[-nrow(z), -ncol(z)])/4
# Range of the facet center on a 100-scale (number of colors)
z.facet.range<-cut(z.facet.center, 100)
#persp(x, y, z,
#      theta = 30, phi = 30, expand = 0.2, scale=FALSE,
#      shade=NA, col=colors[z.facet.range], border="grey80",
#      box=FALSE)
nlevels<-20
my_zlim <- range(z, finite = TRUE)
my_levels <- pretty(my_zlim, nlevels)
cols <- jet.colors(length(my_levels) - 1)
zzz <- cut(z.facet.center, breaks = my_levels, labels = cols)
persp(x, y, z, theta = -25, phi = 45, expand = 0.5,xlab="x",ylab="y",zlab="f(x,y)",col = as.character(zzz))
}
x <- seq(-3,3,0.2)
y <- seq(-3,3,0.2)
s <- 1
z <- outer(x, y, function(x,y) 25/s^4/pi*(1-(x^2+y^2)/2/s^2)*exp(-(x^2+y^2)/2/s^2)) # -ve LOG
plot.3d(x, y, z)
plot.3d <- function(x, y, z) {
# Color palette (100 colors)
col.pal<-colorRampPalette(c("blue", "red"))
colors<-col.pal(100)
# height of facets
z.facet.center <- (z[-1, -1] + z[-1, -ncol(z)] + z[-nrow(z), -1] + z[-nrow(z), -ncol(z)])/4
# Range of the facet center on a 100-scale (number of colors)
z.facet.range<-cut(z.facet.center, 100)
#persp(x, y, z,
#      theta = 30, phi = 30, expand = 0.2, scale=FALSE,
#      shade=NA, col=colors[z.facet.range], border="grey80",
#      box=FALSE)
nlevels<-20
my_zlim <- range(z, finite = TRUE)
my_levels <- pretty(my_zlim, nlevels)
cols <- jet.colors(length(my_levels) - 1)
zzz <- cut(z.facet.center, breaks = my_levels, labels = cols)
persp(x, y, z, theta = -25, phi = 45, expand = 0.5,xlab="x",ylab="y",zlab="f(x,y)",col = as.character(zzz))
}
x <- seq(-3,3,0.1)
y <- seq(-3,3,0.1)
s <- 1
z <- outer(x, y, function(x,y) 25/s^4/pi*(1-(x^2+y^2)/2/s^2)*exp(-(x^2+y^2)/2/s^2)) # -ve LOG
plot.3d(x, y, z)
plot.3d <- function(x, y, z) {
# Color palette (100 colors)
col.pal<-colorRampPalette(c("blue", "red"))
colors<-col.pal(100)
# height of facets
z.facet.center <- (z[-1, -1] + z[-1, -ncol(z)] + z[-nrow(z), -1] + z[-nrow(z), -ncol(z)])/4
# Range of the facet center on a 100-scale (number of colors)
z.facet.range<-cut(z.facet.center, 100)
#persp(x, y, z,
#      theta = 30, phi = 30, expand = 0.2, scale=FALSE,
#      shade=NA, col=colors[z.facet.range], border="grey80",
#      box=FALSE)
nlevels<-50
my_zlim <- range(z, finite = TRUE)
my_levels <- pretty(my_zlim, nlevels)
cols <- jet.colors(length(my_levels) - 1)
zzz <- cut(z.facet.center, breaks = my_levels, labels = cols)
persp(x, y, z, theta = -25, phi = 45, expand = 0.5,xlab="x",ylab="y",zlab="f(x,y)",col = as.character(zzz))
}
x <- seq(-3,3,0.1)
y <- seq(-3,3,0.1)
s <- 1
z <- outer(x, y, function(x,y) 25/s^4/pi*(1-(x^2+y^2)/2/s^2)*exp(-(x^2+y^2)/2/s^2)) # -ve LOG
plot.3d(x, y, z)
plot.3d <- function(x, y, z) {
# Color palette (100 colors)
col.pal<-colorRampPalette(c("blue", "red"))
colors<-col.pal(100)
# height of facets
z.facet.center <- (z[-1, -1] + z[-1, -ncol(z)] + z[-nrow(z), -1] + z[-nrow(z), -ncol(z)])/4
# Range of the facet center on a 100-scale (number of colors)
z.facet.range<-cut(z.facet.center, 100)
#persp(x, y, z,
#      theta = 30, phi = 30, expand = 0.2, scale=FALSE,
#      shade=NA, col=colors[z.facet.range], border="grey80",
#      box=FALSE)
nlevels<-50
my_zlim <- range(z, finite = TRUE)
my_levels <- pretty(my_zlim, nlevels)
cols <- jet.colors(length(my_levels) - 1)
zzz <- cut(z.facet.center, breaks = my_levels, labels = cols)
persp(x, y, z, theta = -25, phi = 45, expand = 0.5,xlab="x",ylab="y",zlab="f(x,y)",col = as.character(zzz), box=FALSE)
}
x <- seq(-3,3,0.1)
y <- seq(-3,3,0.1)
s <- 1
z <- outer(x, y, function(x,y) 25/s^4/pi*(1-(x^2+y^2)/2/s^2)*exp(-(x^2+y^2)/2/s^2)) # -ve LOG
plot.3d(x, y, z)
plot.3d <- function(x, y, z) {
# Color palette (100 colors)
col.pal<-colorRampPalette(c("blue", "red"))
colors<-col.pal(100)
# height of facets
z.facet.center <- (z[-1, -1] + z[-1, -ncol(z)] + z[-nrow(z), -1] + z[-nrow(z), -ncol(z)])/4
# Range of the facet center on a 100-scale (number of colors)
z.facet.range<-cut(z.facet.center, 100)
#persp(x, y, z,
#      theta = 30, phi = 30, expand = 0.2, scale=FALSE,
#      shade=NA, col=colors[z.facet.range], border="grey80",
#      box=FALSE)
nlevels<-50
my_zlim <- range(z, finite = TRUE)
my_levels <- pretty(my_zlim, nlevels)
cols <- jet.colors(length(my_levels) - 1)
zzz <- cut(z.facet.center, breaks = my_levels, labels = cols)
persp(x, y, z, theta = 30, phi = 45, expand = 0.5,xlab="x",ylab="y",zlab="f(x,y)",col = as.character(zzz), box=FALSE)
}
x <- seq(-3,3,0.1)
y <- seq(-3,3,0.1)
s <- 1
z <- outer(x, y, function(x,y) 25/s^4/pi*(1-(x^2+y^2)/2/s^2)*exp(-(x^2+y^2)/2/s^2)) # -ve LOG
plot.3d(x, y, z)
plot.3d <- function(x, y, z) {
# Color palette (100 colors)
col.pal<-colorRampPalette(c("blue", "red"))
colors<-col.pal(100)
# height of facets
z.facet.center <- (z[-1, -1] + z[-1, -ncol(z)] + z[-nrow(z), -1] + z[-nrow(z), -ncol(z)])/4
# Range of the facet center on a 100-scale (number of colors)
z.facet.range<-cut(z.facet.center, 100)
#persp(x, y, z,
#      theta = 30, phi = 30, expand = 0.2, scale=FALSE,
#      shade=NA, col=colors[z.facet.range], border="grey80",
#      box=FALSE)
nlevels<-50
my_zlim <- range(z, finite = TRUE)
my_levels <- pretty(my_zlim, nlevels)
cols <- jet.colors(length(my_levels) - 1)
zzz <- cut(z.facet.center, breaks = my_levels, labels = cols)
persp(x, y, z, theta = 30, phi = 45, expand = 0.5,xlab="x",ylab="y",zlab="f(x,y)",col = as.character(zzz), box=FALSE)
}
x <- seq(-4,4,0.1)
y <- seq(-4,4,0.1)
s <- 1
z <- outer(x, y, function(x,y) 25/s^4/pi*(1-(x^2+y^2)/2/s^2)*exp(-(x^2+y^2)/2/s^2)) # -ve LOG
plot.3d(x, y, z)
plot.3d <- function(x, y, z) {
# Color palette (100 colors)
col.pal<-colorRampPalette(c("blue", "red"))
colors<-col.pal(100)
# height of facets
z.facet.center <- (z[-1, -1] + z[-1, -ncol(z)] + z[-nrow(z), -1] + z[-nrow(z), -ncol(z)])/4
# Range of the facet center on a 100-scale (number of colors)
z.facet.range<-cut(z.facet.center, 100)
#persp(x, y, z,
#      theta = 30, phi = 30, expand = 0.2, scale=FALSE,
#      shade=NA, col=colors[z.facet.range], border="grey80",
#      box=FALSE)
nlevels<-50
my_zlim <- range(z, finite = TRUE)
my_levels <- pretty(my_zlim, nlevels)
cols <- jet.colors(length(my_levels) - 1)
zzz <- cut(z.facet.center, breaks = my_levels, labels = cols)
persp(x, y, z, theta = 30, phi = 45, expand = 0.5,xlab="x",ylab="y",zlab="f(x,y)",col = as.character(zzz), box=FALSE)
}
x <- seq(-3,3,0.1)
y <- seq(-3,3,0.1)
s <- 1
z <- outer(x, y, function(x,y) 25/s^4/pi*(1-(x^2+y^2)/2/s^2)*exp(-(x^2+y^2)/2/s^2)) # -ve LOG
plot.3d(x, y, z)
plot.3d <- function(x, y, z) {
# Color palette (100 colors)
#col.pal<-colorRampPalette(c("blue", "red"))
#colors<-col.pal(100)
cols <- jet.colors(100)
# height of facets
z.facet.center <- (z[-1, -1] + z[-1, -ncol(z)] + z[-nrow(z), -1] + z[-nrow(z), -ncol(z)])/4
# Range of the facet center on a 100-scale (number of colors)
z.facet.range<-cut(z.facet.center, 100)
persp(x, y, z,
theta = 30, phi = 30, expand = 0.2, scale=FALSE,
shade=NA, col=colors[z.facet.range], border="grey80",
box=FALSE)
#nlevels<-50
#my_zlim <- range(z, finite = TRUE)
#my_levels <- pretty(my_zlim, nlevels)
#cols <- jet.colors(length(my_levels) - 1)
#zzz <- cut(z.facet.center, breaks = my_levels, labels = cols)
#persp(x, y, z, theta = 30, phi = 45, expand = 0.5,xlab="x",ylab="y",zlab="f(x,y)",col = as.character(zzz), box=FALSE)
}
x <- seq(-3,3,0.1)
y <- seq(-3,3,0.1)
s <- 1
z <- outer(x, y, function(x,y) 25/s^4/pi*(1-(x^2+y^2)/2/s^2)*exp(-(x^2+y^2)/2/s^2)) # -ve LOG
plot.3d(x, y, z)
plot.3d <- function(x, y, z) {
# Color palette (100 colors)
#col.pal<-colorRampPalette(c("blue", "red"))
#colors<-col.pal(100)
colors <- jet.colors(100)
# height of facets
z.facet.center <- (z[-1, -1] + z[-1, -ncol(z)] + z[-nrow(z), -1] + z[-nrow(z), -ncol(z)])/4
# Range of the facet center on a 100-scale (number of colors)
z.facet.range<-cut(z.facet.center, 100)
persp(x, y, z,
theta = 30, phi = 30, expand = 0.2, scale=FALSE,
shade=NA, col=colors[z.facet.range], border="grey80",
box=FALSE)
#nlevels<-50
#my_zlim <- range(z, finite = TRUE)
#my_levels <- pretty(my_zlim, nlevels)
#cols <- jet.colors(length(my_levels) - 1)
#zzz <- cut(z.facet.center, breaks = my_levels, labels = cols)
#persp(x, y, z, theta = 30, phi = 45, expand = 0.5,xlab="x",ylab="y",zlab="f(x,y)",col = as.character(zzz), box=FALSE)
}
x <- seq(-3,3,0.1)
y <- seq(-3,3,0.1)
s <- 1
z <- outer(x, y, function(x,y) 25/s^4/pi*(1-(x^2+y^2)/2/s^2)*exp(-(x^2+y^2)/2/s^2)) # -ve LOG
plot.3d(x, y, z)
plot.3d <- function(x, y, z) {
# Color palette (100 colors)
#col.pal<-colorRampPalette(c("blue", "red"))
#colors<-col.pal(100)
colors <- jet.colors(100)
# height of facets
z.facet.center <- (z[-1, -1] + z[-1, -ncol(z)] + z[-nrow(z), -1] + z[-nrow(z), -ncol(z)])/4
# Range of the facet center on a 100-scale (number of colors)
z.facet.range<-cut(z.facet.center, 100)
persp(x, y, z,
theta = 30, phi = 30, expand = 0.2, scale=FALSE,
shade=NA, col=colors[z.facet.range], border="grey80",
box=FALSE)
#nlevels<-50
#my_zlim <- range(z, finite = TRUE)
#my_levels <- pretty(my_zlim, nlevels)
#cols <- jet.colors(length(my_levels) - 1)
#zzz <- cut(z.facet.center, breaks = my_levels, labels = cols)
#persp(x, y, z, theta = 30, phi = 45, expand = 0.5,xlab="x",ylab="y",zlab="f(x,y)",col = as.character(zzz), box=FALSE)
}
x <- seq(-3,3,0.2)
y <- seq(-3,3,0.2)
s <- 1
z <- outer(x, y, function(x,y) 25/s^4/pi*(1-(x^2+y^2)/2/s^2)*exp(-(x^2+y^2)/2/s^2)) # -ve LOG
plot.3d(x, y, z)
plot.3d <- function(x, y, z) {
# Color palette (100 colors)
#col.pal<-colorRampPalette(c("blue", "red"))
#colors<-col.pal(100)
colors <- jet.colors(50)
# height of facets
z.facet.center <- (z[-1, -1] + z[-1, -ncol(z)] + z[-nrow(z), -1] + z[-nrow(z), -ncol(z)])/4
# Range of the facet center on a 100-scale (number of colors)
z.facet.range<-cut(z.facet.center, 50)
persp(x, y, z,
theta = 30, phi = 30, expand = 0.2, scale=FALSE,
shade=NA, col=colors[z.facet.range], border="grey80",
box=FALSE)
#nlevels<-50
#my_zlim <- range(z, finite = TRUE)
#my_levels <- pretty(my_zlim, nlevels)
#cols <- jet.colors(length(my_levels) - 1)
#zzz <- cut(z.facet.center, breaks = my_levels, labels = cols)
#persp(x, y, z, theta = 30, phi = 45, expand = 0.5,xlab="x",ylab="y",zlab="f(x,y)",col = as.character(zzz), box=FALSE)
}
x <- seq(-3,3,0.2)
y <- seq(-3,3,0.2)
s <- 1
z <- outer(x, y, function(x,y) 25/s^4/pi*(1-(x^2+y^2)/2/s^2)*exp(-(x^2+y^2)/2/s^2)) # -ve LOG
plot.3d(x, y, z)
?jet.colors
